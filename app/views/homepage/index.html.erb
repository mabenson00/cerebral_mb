<%= react_component('AppRouter',
                    props={user_email: @current_user&.email, user_id: @current_user&.id},
                    {prerender: false, insurance: @current_user&.insurance})
                    %>

