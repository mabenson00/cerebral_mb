<%= react_component('AppRouter',
                    props={user_email: @current_user&.email, user_id: @current_user&.id,  insurance: @current_user&.insurance},
                    {prerender: false})
                    %>

